// Create a name for an existing type.

type Alias1 = string | string[] | null;

type Alias2 = { a: number } & { b: number };

// Use aliases to describe complex types.
type Alias4 = {
  a: number;
  b: number;
};
